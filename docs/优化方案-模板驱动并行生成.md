# 模板驱动并行文档生成 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `cultivation-worldbuilding-research` 从“单模板候选名词采集管线”扩展为“模板目录 + 原文 → 每模板一份子 agent prompt → 并行生成多篇世界观分析文档”的可执行编排能力。

**Architecture:** Python 只负责模板发现、模板画像、run-local framework 预生成、`batch-plan.json` 生成和可测试的 prompt 契约，不直接调用 LLM、不管理 API key、不负责并发执行。并行由调用方根据 `batch-plan.json` 一次性派发多个 subagent；每个 subagent 只处理一个模板和一篇输出文档。模板结构优先从 Markdown 推荐结构中推断，YAML presets/registry 只作兜底配置。

**Tech Stack:** Python 3、pytest、PowerShell 5.1+、Markdown skill 文档、YAML 配置、现有 `scripts/worldbuilding_pipeline.py` CLI。

---

## 前置约束与成功标准

**非目标：**
- 不在 Python 中调用 LLM、Codex、Claude 或任何 agent API。
- 不在 Python 中实现真正的文档 merge 写作逻辑；merge 只通过 `batch-plan.json` 与 subagent prompt 约束执行。
- 不重构旧的候选抽取、review-pack、render 管线，除非为新批量计划提供兼容入口。

**Git 安全约束：**
- 各任务中的提交步骤是检查点建议，仅在用户明确要求提交时执行。
- 如果按任务逐个提交，则不要再执行“最终提交”；如果未逐任务提交且用户要求提交，只 stage 本计划列出的精确文件。
- 不使用目录级 `git add tests`，避免把无关测试改动带入。

**通用架构检查：**
- 批量编排核心放入 `scripts/pipeline/batch_plan.py`，PowerShell 只是薄封装，避免把逻辑写死在脚本里。
- 模板形状判定由 `template_profile.py` 统一负责，`batch_plan.py`、`analysis_framework.py` 和旧 CLI 不复制判定规则。
- `route_template()` 不再依赖丹药模板硬编码；当 registry 无命中时从模板画像与配置兜底生成通用 route。

**设计模式适配检查：**
- 使用“Strategy via config”：不同 `report_shape` 的默认 mode、候选策略、渲染块和字段来自 `assets/framework-presets.yaml`。
- 使用“Builder”：`batch_plan.py` 负责把模板、README 元规则、framework 输出、覆盖策略组装为稳定 JSON 契约。
- 使用“Adapter”：`scripts/run_batch.ps1` 只适配 Windows/PowerShell 调用，不承载业务判断。

**配置化覆盖范围：**
- 模板文件名到 shape 的兜底映射放在 `assets/framework-presets.yaml`。
- shape 到 legacy primary mode 的映射放在 `assets/framework-presets.yaml`。
- prompt contract、mode contract、质量基线、禁止项列表、事实标签、检索方式放在 `assets/batch-prompt-contract.yaml`。
- mode 行为只允许 `overwrite` 和 `merge`，具体文案由 `assets/batch-prompt-contract.yaml` 的 `mode_contracts` 配置。
- `batch_plan.py` 只做 Builder 组装：读取配置、注入模板全文/README/路径/shape，不把领域规则列表写死在 Python 中。

**验收标准：**
- 给定模板目录、原文目录、原文文件，生成一个 `batch-plan.json`。
- `batch-plan.json` 排除 `README.md`，每个模板对应一个 item，item 包含模板路径、推断 shape、输出路径、输出是否存在、mode、framework 输出、subagent prompt。
- 输出文件名规则为“模板名删除 `模板` 二字后加 `.md`”，例如 `丹药分析模板.md` → `丹药分析.md`。
- `skills/extracting-worldbuilding/SKILL.md` 明确“一模板一 agent、并行、禁止单 agent 串跑多篇”。
- `-Mode overwrite` 为默认；`-Mode merge` 的 prompt 明确读取旧文档、保留高质量内容、写本次变更摘要和备份要求。
- 新增模板无需改 registry 即可通过推荐结构被画像和路由。
- `pytest` 通过；至少覆盖 template profile、batch plan、CLI、wrapper、skill 文档和 registry 泛化行为。

## 文件结构

**创建：**
- `assets/batch-prompt-contract.yaml`：subagent prompt 的可配置契约，包含并行约束、执行要求、mode 文案、质量基线和禁止项。
- `scripts/pipeline/batch_plan.py`：批量模板编排的纯 Python 核心，负责模板发现、输出路径派生、framework 预生成、subagent prompt 构造、JSON 写出。
- `scripts/run_batch.ps1`：Windows 入口，接收 `-TemplateDir -SourceDir -SourceFile -Mode`，调用 Python CLI 生成 `batch-plan.json`。
- `tests/test_batch_plan.py`：核心 batch plan 与 merge prompt 契约测试。

**修改：**
- `skills/extracting-worldbuilding/SKILL.md`：重写为模板驱动并行编排说明书，保留旧 Python 管线为辅助能力说明。
- `assets/framework-presets.yaml`：修正真实模板文件名兜底映射，新增 route 默认配置。
- `assets/template-registry.yaml`：移除活动丹药特化，保留非活动示例或空 registry。
- `scripts/pipeline/template_profile.py`：让推荐结构推断优先于 `expected_files`；配置仅兜底。
- `scripts/pipeline/template_router.py`：registry 未命中时基于模板画像生成通用 route。
- `scripts/worldbuilding_pipeline.py`：新增 `batch-plan` CLI 子命令。
- `tests/test_template_profile.py`：补充“推荐结构胜过错误配置”的回归测试。
- `tests/test_real_template_shapes.py`：把真实模板兜底表更新为方案中的模板清单。
- `tests/test_skill_workflow_doc.py`：替换旧 shard contract 断言为新并行编排断言。
- `tests/test_template_router.py`：验证 registry 为空时仍可泛化 route。
- `tests/test_generic_pipeline_cli.py`：验证 `batch-plan` CLI。
- `tests/test_generic_wrapper_contract.py`：验证 `run_batch.ps1` 参数与薄封装契约。

---

### Task 1: 重写技能文档为并行编排契约

**Files:**
- Modify: `skills/extracting-worldbuilding/SKILL.md`
- Modify: `tests/test_skill_workflow_doc.py`

- [ ] **Step 1: 写失败测试**

将 `tests/test_skill_workflow_doc.py` 替换为以下内容：

```python
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
SKILL = ROOT / "skills" / "extracting-worldbuilding" / "SKILL.md"


def test_skill_documents_template_batch_orchestration():
    text = SKILL.read_text(encoding="utf-8")

    assert "batch-plan.json" in text
    assert "每个模板一个独立 subagent" in text
    assert "并行" in text
    assert "禁止一个 agent 串跑多篇文档" in text
    assert "README.md" in text
    assert "文件名 = 模板名删除“模板”二字" in text


def test_skill_documents_subagent_prompt_contract():
    text = SKILL.read_text(encoding="utf-8")

    for required in [
        "模板全文",
        "全局元规则",
        "原文路径",
        "输出路径",
        "原文未说明",
        "待核验",
        "原作事实",
        "我的判断",
    ]:
        assert required in text


def test_skill_documents_overwrite_and_merge_modes():
    text = SKILL.read_text(encoding="utf-8")

    assert "默认 overwrite" in text
    assert "Mode merge" in text
    assert "本次变更摘要" in text
    assert ".bak" in text


def test_skill_documents_quality_baseline_and_python_downgrade():
    text = SKILL.read_text(encoding="utf-8")

    assert "凡人修仙传/法宝妖兽丹药分析.md" in text
    assert "candidate/evidence 抽取仅为 entity_table 类模板" in text
    assert "叙事 / 关系 / 流程类模板" in text
    assert "不调用 LLM" in text
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_skill_workflow_doc.py -q
```

Expected: FAIL，至少有断言找不到 `batch-plan.json`、`每个模板一个独立 subagent` 或 `Mode merge`。

- [ ] **Step 3: 替换技能文档正文**

将 `skills/extracting-worldbuilding/SKILL.md` 保留 frontmatter，正文替换为：

```markdown
---
name: extracting-worldbuilding
description: Use when extracting worldbuilding research from long fiction or game text with configurable templates, evidence packs, and Markdown report validation.
---

# Extracting Worldbuilding

本技能用于“模板目录 + 小说原文”的世界观研究生成。核心流程是模板驱动的并行编排：Python 生成 `batch-plan.json`，调用方根据 plan 派发多个 subagent，最终每个模板产出一篇 Markdown 文档。

## 批量模板编排主流程

输入：模板目录、原文目录、原文文件，可选 `Mode overwrite|merge`。

1. 列出模板目录下所有 `*.md`，排除 `README.md`。
2. 读取模板目录下的 `README.md` 作为全局元规则，并注入每个子任务。
3. 对每个模板创建一个独立任务；每个模板一个独立 subagent，只负责这一份模板对应的一篇文档。
4. 调用方在一条消息内并行派发这些 subagent。
5. 每篇结果写回原文目录，文件名 = 模板名删除“模板”二字，例如 `丹药分析模板.md` 写为 `丹药分析.md`。

强制约束：一模板一 agent；并行；禁止一个 agent 串跑多篇文档。

Python 脚本只生成 `batch-plan.json` 和每模板 framework 文件，不调用 LLM，不保存 API key，不管理并发，不替 subagent 撰写最终文档。

## 推荐入口

在 Windows 上优先使用批量入口：

```powershell
& 'C:\Users\Administrator\plugins\cultivation-worldbuilding-research\scripts\run_batch.ps1' `
  -TemplateDir 'E:\AI_Projects\CultivationWorld\docs\世界观参考\模板' `
  -SourceDir 'E:\AI_Projects\CultivationWorld\凡人修仙传' `
  -SourceFile 'E:\AI_Projects\CultivationWorld\凡人修仙传\凡人修仙传.txt' `
  -Mode overwrite
```

生成后读取原文目录下的 `batch-plan.json`，按 `items[].subagent_prompt` 并行派发 subagent。

## 子 agent 标准 prompt 契约

每个 subagent 必须只收到并只处理一个模板。prompt 必须包含：

- 模板全文，包括适用范围、前置声明、必须写什么、推荐结构。
- `README.md` 全局元规则。
- 原文路径、可用检索方式和输出路径。
- 覆盖模式：默认 overwrite；用户明确要求增量时使用 Mode merge。
- 硬约束：按推荐结构骨架输出；字段无依据写“原文未说明”或“待核验”；禁止为补齐字段编造；区分“原作事实 / 我的判断 / 待核验”；不粘贴大段原文，只给章节/行号和短摘要。

subagent 输出必须直接写入 plan 指定的输出路径。若发现模板与原文完全不匹配，也要写出一份说明文档，列出无法完成的字段和需要用户确认的问题。

## 覆盖与增量约束

默认 overwrite：可以重写目标文档，但仍必须遵循模板推荐结构和证据规则。

Mode merge：必须先读取旧文档，把它视为已有人工成果；保留高质量内容，只补缺、纠错、追加遗漏证据。发生大改写时，先在文档开头写“本次变更摘要”，并把旧文件保存为同目录 `.bak`，或移至文末“历史版本”小节。不得静默推倒已有人工文档。

## 质量基线

输出质量参考 `凡人修仙传/法宝妖兽丹药分析.md`：关注结构密度、体系分层、核心条目表、机制链和证据，而不是模板示例行里的占位内容。

所有结论必须能回溯到原文证据。没有来源的信息使用“原文未说明”或“待核验”，不要为了完整性虚构。

## Python 辅助边界

candidate/evidence 抽取仅为 entity_table 类模板寻找漏网名词和证据片段。叙事 / 关系 / 流程类模板，例如事件因果链、人物关系、修炼历程、职业闭环，可直接由 subagent 检索原文并撰写。

旧的 review-pack、review shard、finalize-reviewed 管线仍可作为单模板实体表工作流使用，但它不是批量文档生成主流程。

## Template-First Flow

当只处理单个模板时，先运行 `profile-template` 或 `prepare-framework`。如果模板画像 confidence 低于配置阈值，先向用户确认 shape、字段和禁止输出形式。

## Windows Runtime Rules

- Always set UTF-8 console and `PYTHONIOENCODING=utf-8` before invoking Python.
- Do not pipe PowerShell here-strings directly into `python -`; BOM bytes can break the first Python token.
- Put multi-line Python probes in a `.py` file or use an existing wrapper.
- Runtime extraction should not run pytest, plugin validation, cachebuster updates, or git commits.
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_skill_workflow_doc.py -q
```

Expected: PASS。

- [ ] **Step 5: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add skills/extracting-worldbuilding/SKILL.md tests/test_skill_workflow_doc.py
git commit -m "docs: define template batch orchestration skill"
```

---

### Task 2: 更新 framework presets 的模板兜底表与 route 默认配置

**Files:**
- Modify: `assets/framework-presets.yaml`
- Modify: `tests/test_analysis_framework.py`
- Modify: `tests/test_real_template_shapes.py`

- [ ] **Step 1: 写失败测试：presets 必须包含真实模板名与 route 默认配置**

在 `tests/test_analysis_framework.py` 的 `test_presets_include_required_config_sections()` 中加入 `route_defaults` 断言，并替换模板名断言为：

```python
def test_presets_include_required_config_sections():
    presets = load_framework_presets(PRESETS)

    assert {
        "template_catalog",
        "template_shapes",
        "template_shape_rules",
        "shape_detection_keywords",
        "subject_aliases",
        "candidate_strategies",
        "render_blocks",
        "validation_rules",
        "route_defaults",
    }.issubset(presets)
    assert presets["template_catalog"]["expected_files"]["README.md"] == "meta_rules"
    assert presets["template_catalog"]["expected_files"]["世界观设定模板.md"] == "overview_plus_cards"
    assert presets["template_catalog"]["expected_files"]["人物关系与事件分析模板.md"] == "relationship_chain"
    assert presets["template_catalog"]["expected_files"]["材料分析模板.md"] == "entity_table"
    assert presets["template_catalog"]["expected_files"]["炼丹师模板.md"] == "profession_workflow"
    assert presets["template_catalog"]["expected_files"]["时间行动与事件耗时模板.md"] == "overview_plus_cards"
    assert presets["route_defaults"]["primary_mode_by_shape"]["entity_table"] == "entity"
    assert presets["route_defaults"]["primary_mode_by_shape"]["process_chain"] == "process"
    assert {"case_collection", "cards_only", "decision_chain", "relationship_chain", "profession_workflow"}.issubset(
        presets["template_shapes"]
    )
    assert all(isinstance(shape_config, dict) for shape_config in presets["template_shapes"].values())
    assert {"case_collection", "cards_only"}.issubset(presets["template_shape_rules"])
    assert {"case_collection", "cards_only"}.issubset(presets["shape_detection_keywords"])
```

- [ ] **Step 2: 写失败测试：真实模板 shape 映射**

> 重要校准说明（执行前必读，已按真实数据修正）：
> - 真实模板目录里**每个模板都带 BOM**。`template_profile.py` 必须用 `utf-8-sig` 读取（见 Task 3 Step 0），否则首行标题会被 BOM 污染。
> - 设计取向按用户决策为 **“推荐结构推断优先、配置仅兜底”**（零配置支持新模板）。因此 `expected_files` 表的作用是：① 给未登记/低置信模板兜底；② 作为人类可读的“理想分类”文档。
> - 实测当前推断器（conf≥0.6）对若干模板的输出与下表“理想 shape”**并不一致**（例：`灵根体质血脉模板.md`→case_collection、`宗门任务体系模板.md`→decision_chain、`妖兽与修士关系分析模板.md`→case_collection、`散修生存方式模板.md`→cards_only、`记忆情绪与执念模板.md`→case_collection）。这是预期的——既然推断优先，就**不能**断言 `profile.report_shape == 配置表值`，否则与 Task 3 的“推断优先”实现直接打架。
> - 因此真正要防回归的是：**(a)** 配置表包含这些键且值合法；**(b)** 每个真实模板都能稳定推断出一个**合法 shape**（不为空、在枚举内）。具体落到哪个 shape 由推断器决定，不在此处钉死。
> - 兜底表里的几个“理想分类”是人工拍的、与推断分歧较大的项（灵根体质血脉/记忆情绪与执念/散修生存方式/宗门任务体系/妖兽与修士关系），执行时**不要把它们当成必须达到的断言目标**；若希望推断结果贴近理想分类，应在 `shape_detection_keywords` 里补关键词调优，而不是改测试去硬等于。

将 `tests/test_real_template_shapes.py` 中的 `test_real_template_shapes_follow_expected_files_config()` 替换为下面**两个**测试。`EXPECTED_SHAPES` 表同时就是 `framework-presets.yaml` 里要写的兜底映射，二者必须保持一致。

```python
VALID_SHAPES = {
    "entity_table",
    "overview_plus_cards",
    "cards_only",
    "case_collection",
    "process_chain",
    "decision_chain",
    "relationship_chain",
    "profession_workflow",
}

# 兜底映射：与 assets/framework-presets.yaml 的 expected_files 一一对应。
# 这是“理想分类”，只用于配置存在性/合法性断言，不强制等于推断器的实时输出。
EXPECTED_SHAPES = {
    "丹药分析模板.md": "entity_table",
    "材料分析模板.md": "entity_table",
    "法宝分析模板.md": "entity_table",
    "武器分析模板.md": "entity_table",
    "妖兽分析模板.md": "entity_table",
    "灵根体质血脉模板.md": "entity_table",
    "功法术法神通模板.md": "cards_only",
    "记忆情绪与执念模板.md": "cards_only",
    "事件因果链（长程因果图）模板.md": "process_chain",
    "境界提升与功法分析模板.md": "process_chain",
    "角色修炼历程模板.md": "process_chain",
    "人物关系与事件分析模板.md": "relationship_chain",
    "妖兽与修士关系分析模板.md": "relationship_chain",
    "角色AI行为参考模板.md": "decision_chain",
    "炼丹师模板.md": "profession_workflow",
    "炼器师模板.md": "profession_workflow",
    "阵法师模板.md": "profession_workflow",
    "符师模板.md": "profession_workflow",
    "拍卖坊市与交易模板.md": "case_collection",
    "冲突事件分析模板.md": "case_collection",
    "相遇剧情与对话设计模板.md": "case_collection",
    "动态事件与机会点模板.md": "case_collection",
    "秘境遗迹与机缘模板.md": "case_collection",
    "夺舍设定分析模板.md": "case_collection",
    "邪修分析模板.md": "case_collection",
    "世界观设定模板.md": "overview_plus_cards",
    "势力设定模板.md": "overview_plus_cards",
    "建筑设施与场所功能模板.md": "overview_plus_cards",
    "世界状态与灾变模板.md": "overview_plus_cards",
    "散修生存方式模板.md": "overview_plus_cards",
    "宗门任务体系模板.md": "overview_plus_cards",
    "信息传播与情报模板.md": "overview_plus_cards",
    "有限视角与叙事日志模板.md": "overview_plus_cards",
    "战斗与保命机制模板.md": "overview_plus_cards",
    "物资产出与消耗模板.md": "overview_plus_cards",
    "出门游历流程分析模板.md": "overview_plus_cards",
    "时间行动与事件耗时模板.md": "overview_plus_cards",
}


def test_expected_files_config_matches_catalog_table():
    """配置表必须包含这些键，且 shape 合法。"""
    configured = load_yaml(PRESETS)["template_catalog"]["expected_files"]
    for name, expected_shape in EXPECTED_SHAPES.items():
        assert name in configured, name
        assert configured[name] == expected_shape, name
        assert expected_shape in VALID_SHAPES, name


def test_real_templates_resolve_to_a_valid_shape():
    """每个真实模板都应稳定推断出一个合法 shape（非空、在枚举内）；
    具体 shape 由推断器决定，不在此钉死。未提供真实模板目录时跳过。"""
    if not TEMPLATE_DIR.exists():
        import pytest

        pytest.skip("real template dir not available")
    for name in EXPECTED_SHAPES:
        path = TEMPLATE_DIR / name
        if not path.exists():
            continue
        profile = build_template_profile(path, presets_path=PRESETS)
        assert profile.report_shape in VALID_SHAPES, f"{name} -> {profile.report_shape!r}"
```

> 说明：这与 Task 3 的“推断优先、配置兜底”一致——不断言 `profile.report_shape == 配置值`（实测会分歧），只保证每个真实模板都能落到一个合法 shape，从而 batch plan 永远能为它生成 framework 与 prompt。`TEMPLATE_DIR` 取真实模板目录（如 `E:/AI_Projects/CultivationWorld/docs/世界观参考/模板`），文件头部 `import` 保持原样，新增 `load_yaml`、`build_template_profile` 引用（若文件未导入则补上）。

- [ ] **Step 3: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_analysis_framework.py::test_presets_include_required_config_sections tests/test_real_template_shapes.py::test_real_template_shapes_follow_expected_files_config -q
```

Expected: FAIL，现有 YAML 缺少多个真实模板名与 `route_defaults`。

- [ ] **Step 4: 修改 YAML**

在 `assets/framework-presets.yaml` 中将 `template_catalog.expected_files` 替换为：

```yaml
template_catalog:
  template_dir: templates
  expected_files:
    README.md: meta_rules
    丹药分析模板.md: entity_table
    材料分析模板.md: entity_table
    法宝分析模板.md: entity_table
    武器分析模板.md: entity_table
    妖兽分析模板.md: entity_table
    灵根体质血脉模板.md: entity_table
    功法术法神通模板.md: cards_only
    记忆情绪与执念模板.md: cards_only
    事件因果链（长程因果图）模板.md: process_chain
    境界提升与功法分析模板.md: process_chain
    角色修炼历程模板.md: process_chain
    人物关系与事件分析模板.md: relationship_chain
    妖兽与修士关系分析模板.md: relationship_chain
    角色AI行为参考模板.md: decision_chain
    炼丹师模板.md: profession_workflow
    炼器师模板.md: profession_workflow
    阵法师模板.md: profession_workflow
    符师模板.md: profession_workflow
    拍卖坊市与交易模板.md: case_collection
    冲突事件分析模板.md: case_collection
    相遇剧情与对话设计模板.md: case_collection
    动态事件与机会点模板.md: case_collection
    秘境遗迹与机缘模板.md: case_collection
    夺舍设定分析模板.md: case_collection
    邪修分析模板.md: case_collection
    世界观设定模板.md: overview_plus_cards
    势力设定模板.md: overview_plus_cards
    建筑设施与场所功能模板.md: overview_plus_cards
    世界状态与灾变模板.md: overview_plus_cards
    散修生存方式模板.md: overview_plus_cards
    宗门任务体系模板.md: overview_plus_cards
    信息传播与情报模板.md: overview_plus_cards
    有限视角与叙事日志模板.md: overview_plus_cards
    战斗与保命机制模板.md: overview_plus_cards
    物资产出与消耗模板.md: overview_plus_cards
    出门游历流程分析模板.md: overview_plus_cards
    时间行动与事件耗时模板.md: overview_plus_cards

route_defaults:
  primary_mode_by_shape:
    entity_table: entity
    overview_plus_cards: overview
    cards_only: card
    case_collection: case
    process_chain: process
    decision_chain: decision
    relationship_chain: relationship
    profession_workflow: workflow
  output_name_pattern: "{work_title}{template_subject}.md"
  report_title_pattern: "《{work_title}》{subject_type}分析"
```

保留文件中现有 `template_shapes`、`template_shape_rules`、`shape_detection_keywords`、`candidate_strategies`、`render_blocks` 和 `validation_rules`。

- [ ] **Step 5: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_analysis_framework.py::test_presets_include_required_config_sections tests/test_real_template_shapes.py::test_real_template_shapes_follow_expected_files_config -q
```

Expected: PASS；如果本机没有 `E:/AI_Projects/.../模板` 的真实模板文件，真实模板画像部分跳过文件检查，但 YAML 配置断言仍必须通过。

- [ ] **Step 6: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add assets/framework-presets.yaml tests/test_analysis_framework.py tests/test_real_template_shapes.py
git commit -m "config: map template catalog shapes"
```

---

### Task 3: 让推荐结构推断优先于配置兜底

**Files:**
- Modify: `scripts/pipeline/template_profile.py`
- Modify: `tests/test_template_profile.py`

- [ ] **Step 0: 修复 BOM 读取（真实模板都带 BOM，必须先做）**

真实模板目录下每个 `*.md` 都带 UTF-8 BOM。`template_profile.py` 当前用 `path.read_text(encoding="utf-8")` 读取，BOM 会污染首行标题（`﻿# 丹药分析模板`），导致标题解析、`template_name`、推断都可能异常。

在 `build_template_profile()` 中把：

```python
    text = path.read_text(encoding="utf-8")
```

改为：

```python
    text = path.read_text(encoding="utf-8-sig")
```

新增回归测试，追加到 `tests/test_template_profile.py`：

```python
def test_template_with_bom_is_parsed(tmp_path):
    path = tmp_path / "丹药分析模板.md"
    path.write_bytes(
        "﻿# 丹药分析模板\n\n## 推荐结构\n\n| 丹药名称 | 功效 | 证据 |\n| --- | --- | --- |\n| 黄龙丹 | 增进修为 | 第一章 |\n".encode("utf-8")
    )

    profile = build_template_profile(path)

    assert profile.template_name == "丹药分析模板"
    assert profile.report_shape == "entity_table"
    assert [field.name for field in profile.fields] == ["丹药名称", "功效", "证据"]
```

> 同样地，Task 5 的 `batch_plan.py` 在读取模板全文注入 prompt 时（`template_path.read_text(...)`）以及读取 `README.md` 时，也必须用 `encoding="utf-8-sig"`，否则注入 subagent 的模板全文/README 首行会带 BOM。执行 Task 5 时一并改。

- [ ] **Step 1: 写失败测试**

在 `tests/test_template_profile.py` 追加：

```python
def test_recommended_structure_inference_overrides_wrong_expected_file_config(tmp_path):
    presets = write_template(
        tmp_path,
        "wrong-presets.yaml",
        """parser:
  recommended_structure_headings: [推荐结构]
  card_section_headings: [卡片]
template_catalog:
  template_dir: .
  expected_files:
    丹药分析模板.md: relationship_chain
template_shape_rules:
  entity_table:
    prefer_when: [recommended_table_has_subject_name_field]
    table_column_min: 4
  relationship_chain:
    prefer_when: [node_edge_or_relation_sections]
    requires_any: [node_edge_table]
shape_detection_keywords:
  entity_table:
    columns: [丹药名称, 功效, 证据]
  relationship_chain:
    columns: [节点, 关系, 对象]
validation_rules:
  low_confidence_threshold: 0.6
""",
    )
    template = write_template(
        tmp_path,
        "丹药分析模板.md",
        """# 丹药分析模板

## 推荐结构

| 丹药名称 | 稀有度 | 功效 | 来源 | 证据 |
| --- | --- | --- | --- | --- |
| 黄龙丹 | 常见 | 增进修为 | 原文 | 第一章 |
""",
    )

    profile = build_template_profile(template, presets)

    assert profile.report_shape == "entity_table"
    assert [field.name for field in profile.fields] == ["丹药名称", "稀有度", "功效", "来源", "证据"]
    assert profile.name_field == "丹药名称"


def test_expected_file_config_wins_when_recommended_section_has_no_classifiable_structure(tmp_path):
    presets = write_template(
        tmp_path,
        "presets.yaml",
        """parser:
  recommended_structure_headings: [推荐结构]
template_catalog:
  template_dir: .
  expected_files:
    角色AI行为参考模板.md: decision_chain
template_shape_rules:
  entity_table:
    prefer_when: [recommended_table_has_subject_name_field]
  decision_chain:
    prefer_when: [choice_or_condition_consequence_sections]
    requires_any: [decision_nodes]
shape_detection_keywords:
  entity_table:
    columns: [名称, 证据]
  decision_chain:
    heading: [决策节点]
validation_rules:
  low_confidence_threshold: 0.6
""",
    )
    # 必须在同目录建 README.md：_configured_report_shape() 仅在模板位于
    # 配置 template_dir 内、或同目录存在 README.md（catalog 语境）时才读取
    # expected_files；否则配置兜底分支永远走不到，confidence==1.0 会失败。
    write_template(tmp_path, "README.md", "# 模板目录\n")
    template = write_template(
        tmp_path,
        "角色AI行为参考模板.md",
        """# 角色AI行为参考模板

## 推荐结构

请整理角色在关键场景中的行为倾向。
""",
    )

    profile = build_template_profile(template, presets)

    assert profile.report_shape == "decision_chain"
    assert profile.confidence == 1.0
```

> 校准说明：`_configured_report_shape()` 通过两个条件之一激活——模板在配置 `template_dir` 内，**或**模板同目录存在 `README.md`。上面的临时 presets 用 `template_dir: .`（解析为插件根的相对目录，不等于 tmp_path），所以**必须**靠同目录 `README.md` 触发 catalog 语境，否则配置兜底永不生效。真实模板目录天然带 `README.md`，无此问题。

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_template_profile.py::test_recommended_structure_inference_overrides_wrong_expected_file_config tests/test_template_profile.py::test_expected_file_config_wins_when_recommended_section_has_no_classifiable_structure -q
```

Expected: FAIL，当前实现会优先返回配置里的 `relationship_chain`。

- [ ] **Step 3: 修改 `build_template_profile()`**

把 `scripts/pipeline/template_profile.py` 中 `build_template_profile()` 的 shape 选择逻辑改为：

```python
def build_template_profile(template_path: Path | str, presets_path: Path | None = None) -> TemplateProfile:
    """Read a template, parse Markdown structures, then classify report_shape from Markdown first and YAML second."""
    path = Path(template_path)
    text = path.read_text(encoding="utf-8")
    rules = load_shape_rules(presets_path)
    snapshot = _parse_markdown(text, rules)
    inferred_shape, inferred_confidence = _classify_shape(snapshot, rules)
    configured_shape = _configured_report_shape(path, rules)
    threshold = float(rules.get("validation_rules", {}).get("low_confidence_threshold", 0.6))

    if inferred_shape and inferred_confidence >= threshold:
        report_shape, confidence = inferred_shape, inferred_confidence
    elif configured_shape:
        report_shape, confidence = configured_shape, 1.0
    else:
        report_shape, confidence = inferred_shape, inferred_confidence

    tables = snapshot.recommended_tables or snapshot.tables
    field_tables = _field_source_tables(report_shape, tables)
    fields = _fields_from_tables(field_tables)
    questions = LOW_CONFIDENCE_QUESTIONS if confidence < threshold else []
    name_field = _detect_name_field(fields) if report_shape in {"entity_table", "overview_plus_cards"} else ""

    return TemplateProfile(
        template_path=str(path),
        template_name=path.stem,
        template_kind=_detect_template_kind(path.stem, snapshot.tables),
        report_shape=report_shape,
        sections=snapshot.table_sections + snapshot.card_sections,
        tables=tables,
        fields=fields,
        name_field=name_field,
        confidence=confidence,
        questions=questions,
        forbidden_output_modes=_detect_forbidden_output_modes(text),
    )
```

- [ ] **Step 4: 运行相关测试**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_template_profile.py tests/test_real_template_shapes.py -q
```

Expected: PASS。

- [ ] **Step 5: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add scripts/pipeline/template_profile.py tests/test_template_profile.py
git commit -m "feat: prefer template recommended structure"
```

---

### Task 4: 泛化 template registry 路由，移除丹药活动硬编码

**Files:**
- Modify: `assets/template-registry.yaml`
- Modify: `scripts/pipeline/template_router.py`
- Modify: `tests/test_template_router.py`
- Modify: `tests/test_pipeline_cli.py`

- [ ] **Step 1: 写失败测试：registry 为空仍能 route**

将 `tests/test_template_router.py` 中 `test_routes_phase1_templates()` 替换为：

```python
def test_routes_template_from_profile_when_registry_has_no_active_template(tmp_path):
    registry = tmp_path / "template-registry.yaml"
    registry.write_text("templates: {}\n", encoding="utf-8")
    template = tmp_path / "丹药分析模板.md"
    template.write_text(
        """# 丹药分析模板

## 推荐结构

| 丹药名称 | 稀有度 | 功效 | 来源 | 证据 |
| --- | --- | --- | --- | --- |
| 示例 | 常见 | 增进修为 | 原文 | 第一章 |
""",
        encoding="utf-8",
    )

    route = route_template(registry, template, "")

    assert route["primary_mode"] == "entity"
    assert route["subject_type"] == "丹药"
    assert route["output_name_pattern"] == "{work_title}丹药分析.md"
    assert route["report_title_pattern"] == "《{work_title}》{subject_type}分析"
    assert route["required_columns"] == ["丹药名称", "稀有度", "功效", "来源", "证据"]
    assert route["template_name"] == "丹药分析模板.md"
    assert route["template_profile"]["report_shape"] == "entity_table"
```

- [ ] **Step 2: 更新 CLI 测试期望**

在 `tests/test_pipeline_cli.py` 的 `test_cli_route_template_accepts_plan_style_aliases_and_ignored_args()` 中，保留 `assert route["subject_type"] == "丹药"`，并新增：

```python
    assert route["output_name_pattern"] == "{work_title}丹药分析.md"
```

若该测试仍期待 `rule_pack == "entity-medicine"`，删除该期待；新行为不再把丹药规则包作为全局默认。

- [ ] **Step 3: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_template_router.py tests/test_pipeline_cli.py::test_cli_route_template_accepts_plan_style_aliases_and_ignored_args -q
```

Expected: FAIL，当前 `route_template()` 在 registry 未命中时会抛 `KeyError`。

- [ ] **Step 4: 修改 `template_router.py`**

将 `scripts/pipeline/template_router.py` 替换为：

```python
from __future__ import annotations

from dataclasses import asdict
from pathlib import Path

from .config_loader import load_yaml
from .template_profile import build_template_profile


DEFAULT_PRESETS = Path(__file__).resolve().parents[2] / "assets" / "framework-presets.yaml"


def route_template(
    registry_path: Path,
    template_path: Path,
    user_request: str = "",
    presets_path: Path | None = None,
) -> dict:
    registry = load_yaml(registry_path)
    name = template_path.name
    templates = registry.get("templates", {})
    if not isinstance(templates, dict):
        raise ValueError("templates must be a mapping")

    if name in templates:
        route = dict(templates[name])
        route["template_name"] = name
        return route

    matches = [key for key in templates if key in user_request]
    if matches:
        matched = matches[0]
        route = dict(templates[matched])
        route["template_name"] = matched
        return route

    return _generic_route(template_path, presets_path or DEFAULT_PRESETS)


def _generic_route(template_path: Path, presets_path: Path) -> dict:
    presets = load_yaml(presets_path)
    profile = build_template_profile(template_path, presets_path=presets_path) if template_path.exists() else None
    shape = profile.report_shape if profile is not None else ""
    route_defaults = presets.get("route_defaults", {})
    primary_mode_by_shape = route_defaults.get("primary_mode_by_shape", {})
    primary_mode = primary_mode_by_shape.get(shape, shape or "generic")
    template_subject = _template_subject(template_path)
    subject_type = _subject_type(template_subject)
    output_pattern = str(route_defaults.get("output_name_pattern", "{work_title}{template_subject}.md")).format(
        work_title="{work_title}",
        template_subject=template_subject,
        subject_type=subject_type,
    )

    return {
        "primary_mode": primary_mode,
        "secondary_modes": [],
        "subject_type": subject_type,
        "rule_pack": "",
        "output_name_pattern": output_pattern,
        "report_title_pattern": str(route_defaults.get("report_title_pattern", "《{work_title}》{subject_type}分析")),
        "required_columns": [field.name for field in profile.fields] if profile is not None else [],
        "template_name": template_path.name,
        "template_profile": asdict(profile) if profile is not None else {},
    }


def _template_subject(template_path: Path) -> str:
    stem = template_path.stem
    return stem.replace("模板", "")


def _subject_type(template_subject: str) -> str:
    return template_subject.removesuffix("分析") or template_subject
```

- [ ] **Step 5: 修改 `assets/template-registry.yaml`**

将活动 registry 改为：

```yaml
templates: {}

examples:
  丹药分析模板.md:
    primary_mode: entity
    secondary_modes: [economy]
    subject_type: 丹药
    rule_pack: entity-medicine
    output_name_pattern: "{work_title}丹药分析.md"
    report_title_pattern: "《{work_title}》{subject_type}分析"
    required_columns: [丹药名称, 稀有度, 功效, 用途, 丹方, 炼制方式, 来源, 限制/副作用, 适用境界]
```

- [ ] **Step 6: 运行测试确认通过（含全量回归，防止 registry 置空连带破坏）**

把 `templates` 置空后，任何隐式依赖真实 registry 命中丹药的测试都会暴露。除了目标测试外，必须跑**整份** `test_template_router.py` 与 `test_pipeline_cli.py`，再加 render 路径相关用例：

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_template_router.py tests/test_pipeline_cli.py -q
```

Expected: PASS。已知安全点（执行前已核对）：`test_pipeline_cli.py` 中 line ~318/989 的 route 字典是测试内联构造，不读 registry；line ~1064 的 `--registry` 传给 `render` 命令而 render 会忽略它（`add_ignored_path_args`）。真正读真实 registry 的消费者只有 `test_template_router.py::test_routes_phase1_templates`（本任务已整体替换）和 `test_cli_route_template_accepts_plan_style_aliases_and_ignored_args`（Step 2 已更新）。若全量跑出现其它失败，多半是新增了依赖真实 registry 的测试，按“配置兜底”语义就地修正，不要回退 registry 置空。

- [ ] **Step 7: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add assets/template-registry.yaml scripts/pipeline/template_router.py tests/test_template_router.py tests/test_pipeline_cli.py
git commit -m "feat: derive generic template routes"
```

---

### Task 5: 新增可配置 batch plan 核心模块

**Files:**
- Create: `assets/batch-prompt-contract.yaml`
- Create: `scripts/pipeline/batch_plan.py`
- Create: `tests/test_batch_plan.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_batch_plan.py`：

```python
import json
from pathlib import Path

import pytest

from scripts.pipeline.batch_plan import build_batch_plan, load_prompt_contract, write_batch_plan


ROOT = Path(__file__).resolve().parents[1]
PROMPT_CONTRACT = ROOT / "assets" / "batch-prompt-contract.yaml"


def write_template(path: Path, text: str) -> Path:
    path.write_text(text, encoding="utf-8")
    return path


def test_prompt_contract_is_configured_in_yaml():
    contract = load_prompt_contract(PROMPT_CONTRACT)

    assert contract["parallel_rule"] == "每个模板一个独立 subagent；你只能处理本 prompt 中的这一个模板，禁止一个 agent 串跑多篇文档。多个同类任务会由调用方并行派发。"
    assert "overwrite" in contract["mode_contracts"]
    assert "merge_existing" in contract["mode_contracts"]
    assert "merge_new" in contract["mode_contracts"]
    assert contract["quality_baseline"]["reference"] == "凡人修仙传/法宝妖兽丹药分析.md"
    assert "原文未说明" in contract["unknown_terms"]
    assert "原作事实" in contract["fact_labels"]
    assert "禁止为补齐境界、丹方、关系、流程或案例而编造内容。" in contract["forbidden_rules"]


def test_build_batch_plan_creates_one_item_per_template(tmp_path):
    template_dir = tmp_path / "templates"
    template_dir.mkdir()
    source_dir = tmp_path / "凡人修仙传"
    source_dir.mkdir()
    source_file = write_template(source_dir / "凡人修仙传.txt", "韩立服下黄龙丹。")
    write_template(template_dir / "README.md", "# 元规则\n\n- 区分原作事实 / 我的判断 / 待核验\n")
    write_template(
        template_dir / "丹药分析模板.md",
        "# 丹药分析模板\n\n## 推荐结构\n\n| 丹药名称 | 功效 | 证据 |\n| --- | --- | --- |\n| 黄龙丹 | 增进修为 | 第一章 |\n",
    )
    write_template(
        template_dir / "事件因果链（长程因果图）模板.md",
        "# 事件因果链（长程因果图）模板\n\n## 推荐结构\n\n1. 起因：\n2. 发展：\n3. 结果：\n\n起因 -> 发展 -> 结果\n",
    )

    plan = build_batch_plan(
        template_dir=template_dir,
        source_dir=source_dir,
        source_file=source_file,
        mode="overwrite",
        framework_root=source_dir / ".worldbuilding-framework",
    )

    assert plan["schema_version"] == 1
    assert plan["mode"] == "overwrite"
    assert plan["template_count"] == 2
    assert [Path(item["template_path"]).name for item in plan["items"]] == [
        "丹药分析模板.md",
        "事件因果链（长程因果图）模板.md",
    ]
    assert [Path(item["output_path"]).name for item in plan["items"]] == [
        "丹药分析.md",
        "事件因果链（长程因果图）.md",
    ]
    assert plan["items"][0]["report_shape"] == "entity_table"
    assert plan["items"][1]["report_shape"] == "process_chain"
    assert "每个模板一个独立 subagent" in plan["items"][0]["subagent_prompt"]
    assert "丹药分析模板.md" in plan["items"][0]["subagent_prompt"]
    assert "事件因果链（长程因果图）模板.md" not in plan["items"][0]["subagent_prompt"]
    assert "区分原作事实 / 我的判断 / 待核验" in plan["items"][0]["subagent_prompt"]
    assert "字段无依据写法：原文未说明 / 待核验" in plan["items"][0]["subagent_prompt"]
    assert "事实标签：原作事实 / 我的判断 / 待核验" in plan["items"][0]["subagent_prompt"]
    assert "禁止为补齐境界、丹方、关系、流程或案例而编造内容。" in plan["items"][0]["subagent_prompt"]


def test_build_batch_plan_merge_mode_records_existing_output_and_merge_rules(tmp_path):
    template_dir = tmp_path / "templates"
    template_dir.mkdir()
    source_dir = tmp_path / "凡人修仙传"
    source_dir.mkdir()
    source_file = write_template(source_dir / "凡人修仙传.txt", "韩立服下黄龙丹。")
    write_template(template_dir / "README.md", "# 元规则\n")
    write_template(
        template_dir / "丹药分析模板.md",
        "# 丹药分析模板\n\n## 推荐结构\n\n| 丹药名称 | 功效 | 证据 |\n| --- | --- | --- |\n",
    )
    write_template(source_dir / "丹药分析.md", "# 旧丹药分析\n\n人工整理内容。\n")

    plan = build_batch_plan(
        template_dir=template_dir,
        source_dir=source_dir,
        source_file=source_file,
        mode="merge",
        framework_root=source_dir / ".worldbuilding-framework",
    )

    [item] = plan["items"]
    assert item["mode"] == "merge"
    assert item["output_exists"] is True
    assert "先读取旧文档" in item["subagent_prompt"]
    assert "本次变更摘要" in item["subagent_prompt"]
    assert ".bak" in item["subagent_prompt"]


def test_build_batch_plan_rejects_unknown_mode(tmp_path):
    template_dir = tmp_path / "templates"
    template_dir.mkdir()
    source_dir = tmp_path / "source"
    source_dir.mkdir()
    source_file = write_template(source_dir / "source.txt", "正文")

    with pytest.raises(ValueError, match="mode must be one of"):
        build_batch_plan(template_dir, source_dir, source_file, mode="append")


def test_write_batch_plan_writes_utf8_json(tmp_path):
    template_dir = tmp_path / "templates"
    template_dir.mkdir()
    source_dir = tmp_path / "source"
    source_dir.mkdir()
    source_file = write_template(source_dir / "凡人修仙传.txt", "正文")
    write_template(template_dir / "README.md", "# 元规则\n")
    write_template(
        template_dir / "世界观设定模板.md",
        "# 世界观设定模板\n\n## 推荐结构\n\n| 名称 | 类型 | 证据 |\n| --- | --- | --- |\n\n## 设定卡：{名称}\n- 类型：\n- 证据：\n",
    )
    output = tmp_path / "batch-plan.json"

    plan = build_batch_plan(template_dir, source_dir, source_file)
    write_batch_plan(output, plan)

    loaded = json.loads(output.read_text(encoding="utf-8"))
    assert loaded["items"][0]["template_name"] == "世界观设定模板.md"
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_batch_plan.py -q
```

Expected: FAIL，`scripts.pipeline.batch_plan` 与 `assets/batch-prompt-contract.yaml` 不存在。

- [ ] **Step 3: 创建 prompt contract 配置**

创建 `assets/batch-prompt-contract.yaml`：

```yaml
version: 1
intro: "你是世界观研究 subagent。"
parallel_rule: "每个模板一个独立 subagent；你只能处理本 prompt 中的这一个模板，禁止一个 agent 串跑多篇文档。多个同类任务会由调用方并行派发。"
search_methods:
  - rg
  - 分段阅读
  - .storygraph
unknown_terms:
  - 原文未说明
  - 待核验
fact_labels:
  - 原作事实
  - 我的判断
  - 待核验
execution_requirements:
  - "阅读下方模板全文，严格按“推荐结构”骨架输出。"
  - "阅读下方 README.md 全局元规则，并应用到本模板。"
  - "检索原文路径中的文本；优先使用配置中的检索方式。"
  - "字段无依据时写“原文未说明”或“待核验”。"
  - "区分“原作事实 / 我的判断 / 待核验”。"
  - "不粘贴大段原文，只给章节/行号和短摘要。"
  - "将最终 Markdown 写入输出路径。"
forbidden_rules:
  - "禁止为补齐境界、丹方、关系、流程或案例而编造内容。"
  - "禁止一个 agent 串跑多篇文档。"
  - "禁止粘贴大段原文。"
quality_baseline:
  reference: "凡人修仙传/法宝妖兽丹药分析.md"
  expectation: "对标其结构密度、体系分层、机制链、核心条目表和证据，而不是模板示例行里的占位内容。"
mode_contracts:
  overwrite: "默认 overwrite：可以重写目标文档，但必须保留模板推荐结构、证据锚点和禁造规则。"
  merge_existing: "Mode merge：先读取旧文档，把它视为已有人工成果；保留高质量内容，只补缺、纠错、追加遗漏证据。发生大改写时，先写“本次变更摘要”，并把旧文件保存为同目录 `.bak`，或移至文末“历史版本”小节。不得静默推倒已有人工文档。"
  merge_new: "Mode merge：目标文档尚不存在，按新文档创建；仍需在文档开头说明本次为首次生成，不需要 `.bak`。"
```

- [ ] **Step 4: 创建 `batch_plan.py`**

创建 `scripts/pipeline/batch_plan.py`：

```python
from __future__ import annotations

from dataclasses import asdict
import json
from pathlib import Path
import re
from typing import Any

from scripts.pipeline.analysis_framework import build_framework
from scripts.pipeline.config_loader import load_yaml
from scripts.pipeline.template_profile import build_template_profile


ROOT = Path(__file__).resolve().parents[2]
DEFAULT_PRESETS = ROOT / "assets" / "framework-presets.yaml"
DEFAULT_PROMPT_CONTRACT = ROOT / "assets" / "batch-prompt-contract.yaml"
VALID_MODES = {"overwrite", "merge"}


def build_batch_plan(
    template_dir: Path,
    source_dir: Path,
    source_file: Path,
    mode: str = "overwrite",
    output_path: Path | None = None,
    framework_root: Path | None = None,
    presets_path: Path | None = None,
    prompt_contract_path: Path | None = None,
) -> dict[str, Any]:
    template_dir = Path(template_dir)
    source_dir = Path(source_dir)
    source_file = Path(source_file)
    presets_path = Path(presets_path) if presets_path is not None else DEFAULT_PRESETS
    prompt_contract_path = Path(prompt_contract_path) if prompt_contract_path is not None else DEFAULT_PROMPT_CONTRACT
    prompt_contract = load_prompt_contract(prompt_contract_path)
    framework_root = Path(framework_root) if framework_root is not None else source_dir / ".worldbuilding-framework"
    output_path = Path(output_path) if output_path is not None else source_dir / "batch-plan.json"
    normalized_mode = mode.lower()
    if normalized_mode not in VALID_MODES:
        raise ValueError(f"mode must be one of {sorted(VALID_MODES)}: {mode}")
    if not template_dir.is_dir():
        raise FileNotFoundError(f"template_dir not found: {template_dir}")
    if not source_dir.is_dir():
        raise FileNotFoundError(f"source_dir not found: {source_dir}")
    if not source_file.is_file():
        raise FileNotFoundError(f"source_file not found: {source_file}")

    readme_path = template_dir / "README.md"
    readme_text = readme_path.read_text(encoding="utf-8-sig") if readme_path.exists() else ""
    templates = list_templates(template_dir)
    items = [
        build_batch_item(
            index=index,
            template_path=template_path,
            source_dir=source_dir,
            source_file=source_file,
            mode=normalized_mode,
            framework_root=framework_root,
            presets_path=presets_path,
            prompt_contract=prompt_contract,
            readme_path=readme_path if readme_path.exists() else None,
            readme_text=readme_text,
        )
        for index, template_path in enumerate(templates, 1)
    ]
    return {
        "schema_version": 1,
        "template_dir": str(template_dir),
        "source_dir": str(source_dir),
        "source_file": str(source_file),
        "mode": normalized_mode,
        "output_path": str(output_path),
        "framework_root": str(framework_root),
        "prompt_contract": str(prompt_contract_path),
        "template_count": len(items),
        "items": items,
    }


def load_prompt_contract(path: Path) -> dict[str, Any]:
    contract = load_yaml(path)
    required = [
        "parallel_rule",
        "search_methods",
        "unknown_terms",
        "fact_labels",
        "execution_requirements",
        "forbidden_rules",
        "quality_baseline",
        "mode_contracts",
    ]
    missing = [key for key in required if key not in contract]
    if missing:
        raise ValueError(f"batch prompt contract missing keys: {missing}")
    return contract


def list_templates(template_dir: Path) -> list[Path]:
    return [
        path
        for path in sorted(template_dir.glob("*.md"), key=lambda item: item.name)
        if path.name.lower() != "readme.md"
    ]


def build_batch_item(
    index: int,
    template_path: Path,
    source_dir: Path,
    source_file: Path,
    mode: str,
    framework_root: Path,
    presets_path: Path,
    prompt_contract: dict[str, Any],
    readme_path: Path | None,
    readme_text: str,
) -> dict[str, Any]:
    output_path = derive_output_path(source_dir, template_path)
    framework_dir = framework_root / f"{index:03d}-{safe_name(template_path.stem)}"
    framework_outputs = build_framework(template_path, presets_path, framework_dir)
    profile = build_template_profile(template_path, presets_path=presets_path)
    template_text = template_path.read_text(encoding="utf-8-sig")
    return {
        "index": index,
        "template_path": str(template_path),
        "template_name": template_path.name,
        "report_shape": profile.report_shape,
        "confidence": profile.confidence,
        "questions": profile.questions,
        "output_path": str(output_path),
        "output_exists": output_path.exists(),
        "mode": mode,
        "framework_dir": str(framework_dir),
        "framework_outputs": {key: str(value) for key, value in framework_outputs.items()},
        "subagent_prompt": build_subagent_prompt(
            template_path=template_path,
            template_text=template_text,
            readme_path=readme_path,
            readme_text=readme_text,
            source_file=source_file,
            output_path=output_path,
            report_shape=profile.report_shape,
            mode=mode,
            output_exists=output_path.exists(),
            prompt_contract=prompt_contract,
        ),
    }


def derive_output_path(source_dir: Path, template_path: Path) -> Path:
    output_stem = template_path.stem.replace("模板", "")
    return source_dir / f"{output_stem}.md"


def safe_name(value: str) -> str:
    cleaned = re.sub(r'[<>:"/\\\\|?*]+', "-", value).strip(". ")
    return cleaned or "template"


def build_subagent_prompt(
    template_path: Path,
    template_text: str,
    readme_path: Path | None,
    readme_text: str,
    source_file: Path,
    output_path: Path,
    report_shape: str,
    mode: str,
    output_exists: bool,
    prompt_contract: dict[str, Any],
) -> str:
    mode_key = "overwrite" if mode == "overwrite" else "merge_existing" if output_exists else "merge_new"
    mode_contract = prompt_contract["mode_contracts"][mode_key]
    readme_label = str(readme_path) if readme_path is not None else "未提供 README.md"
    search_methods = "、".join(str(item) for item in prompt_contract.get("search_methods", []))
    unknown_terms = " / ".join(str(item) for item in prompt_contract["unknown_terms"])
    fact_labels = " / ".join(str(item) for item in prompt_contract["fact_labels"])
    quality = prompt_contract.get("quality_baseline", {})
    return f"""{prompt_contract.get("intro", "")}

{prompt_contract["parallel_rule"]}

任务输入：
- 模板路径：{template_path}
- 推断形状：{report_shape}
- 全局元规则来源：{readme_label}
- 原文路径：{source_file}
- 输出路径：{output_path}
- 写入模式：{mode}
- 可用检索方式：{search_methods}

执行要求：
{format_numbered(prompt_contract["execution_requirements"])}

配置规则：
- 字段无依据写法：{unknown_terms}
- 事实标签：{fact_labels}

禁止规则：
{format_bullets(prompt_contract["forbidden_rules"])}

质量基线：
- 参考：{quality.get("reference", "")}
- 要求：{quality.get("expectation", "")}

覆盖策略：
{mode_contract}

README.md 全局元规则：
```markdown
{readme_text}
```

模板全文：
```markdown
{template_text}
```
"""


def format_numbered(items: list[Any]) -> str:
    return "\n".join(f"{index}. {item}" for index, item in enumerate(items, 1))


def format_bullets(items: list[Any]) -> str:
    return "\n".join(f"- {item}" for item in items)


def write_batch_plan(output_path: Path, plan: dict[str, Any]) -> Path:
    output_path.parent.mkdir(parents=True, exist_ok=True)
    output_path.write_text(json.dumps(plan, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
    return output_path
```

- [ ] **Step 5: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_batch_plan.py -q
```

Expected: PASS。

- [ ] **Step 6: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add assets/batch-prompt-contract.yaml scripts/pipeline/batch_plan.py tests/test_batch_plan.py
git commit -m "feat: build template batch plans"
```

---

### Task 6: 增加 `batch-plan` CLI 子命令

**Files:**
- Modify: `scripts/worldbuilding_pipeline.py`
- Modify: `tests/test_generic_pipeline_cli.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_generic_pipeline_cli.py` 追加：

```python
def test_batch_plan_cli_writes_plan(tmp_path: Path):
    template_dir = tmp_path / "templates"
    template_dir.mkdir()
    source_dir = tmp_path / "凡人修仙传"
    source_dir.mkdir()
    source_file = source_dir / "凡人修仙传.txt"
    source_file.write_text("韩立服下黄龙丹。", encoding="utf-8")
    (template_dir / "README.md").write_text("# 元规则\n", encoding="utf-8")
    (template_dir / "丹药分析模板.md").write_text(
        "# 丹药分析模板\n\n## 推荐结构\n\n| 丹药名称 | 功效 | 证据 |\n| --- | --- | --- |\n",
        encoding="utf-8",
    )
    output = source_dir / "batch-plan.json"

    result = run_cli(
        "batch-plan",
        "--template-dir",
        str(template_dir),
        "--source-dir",
        str(source_dir),
        "--source-file",
        str(source_file),
        "--mode",
        "merge",
        "--output",
        str(output),
        "--prompt-contract",
        str(ROOT / "assets" / "batch-prompt-contract.yaml"),
    )

    assert result.returncode == 0, result.stderr
    stdout = json.loads(result.stdout)
    assert stdout["output"] == str(output)
    assert stdout["items"] == 1
    plan = json.loads(output.read_text(encoding="utf-8"))
    assert plan["mode"] == "merge"
    assert plan["items"][0]["template_name"] == "丹药分析模板.md"
    assert plan["prompt_contract"].endswith("batch-prompt-contract.yaml")
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_generic_pipeline_cli.py::test_batch_plan_cli_writes_plan -q
```

Expected: FAIL，CLI 没有 `batch-plan` 子命令。

- [ ] **Step 3: 修改 imports 与命令函数**

在 `scripts/worldbuilding_pipeline.py` imports 中加入：

```python
from scripts.pipeline.batch_plan import build_batch_plan, write_batch_plan
```

在常量区加入：

```python
BATCH_PLAN_NAME = "batch-plan.json"
```

在 `cmd_prepare_framework()` 后加入：

```python
def cmd_batch_plan(args: argparse.Namespace) -> int:
    output = _path(args.output) or (Path(args.source_dir) / BATCH_PLAN_NAME)
    plan = build_batch_plan(
        template_dir=Path(args.template_dir),
        source_dir=Path(args.source_dir),
        source_file=Path(args.source_file),
        mode=args.mode,
        output_path=output,
        framework_root=_path(args.framework_root),
        prompt_contract_path=_path(args.prompt_contract),
    )
    write_batch_plan(output, plan)
    _stdout_json({"output": str(output), "items": len(plan["items"]), "mode": plan["mode"]})
    return 0
```

- [ ] **Step 4: 修改 parser**

在 `build_parser()` 中 `prepare_framework` 子命令后加入：

```python
    batch_plan = subparsers.add_parser(
        "batch-plan",
        help="Generate a batch-plan.json for template-per-subagent document generation.",
    )
    batch_plan.add_argument("--template-dir", required=True, type=Path)
    batch_plan.add_argument("--source-dir", required=True, type=Path)
    batch_plan.add_argument("--source-file", required=True, type=Path)
    batch_plan.add_argument("--mode", choices=["overwrite", "merge"], default="overwrite")
    batch_plan.add_argument("--output", type=Path)
    batch_plan.add_argument("--framework-root", type=Path)
    batch_plan.add_argument("--prompt-contract", type=Path)
    batch_plan.set_defaults(func=cmd_batch_plan)
```

- [ ] **Step 5: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_generic_pipeline_cli.py::test_batch_plan_cli_writes_plan -q
```

Expected: PASS。

- [ ] **Step 6: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add scripts/worldbuilding_pipeline.py tests/test_generic_pipeline_cli.py
git commit -m "feat: expose batch plan cli"
```

---

### Task 7: 新增 `run_batch.ps1` Windows 薄封装

**Files:**
- Create: `scripts/run_batch.ps1`
- Modify: `tests/test_generic_wrapper_contract.py`

- [ ] **Step 1: 写失败测试**

将 `tests/test_generic_wrapper_contract.py` 顶部 imports 调整为：

```python
import json
from pathlib import Path
import shutil
import subprocess
import sys

import pytest
```

在 `tests/test_generic_wrapper_contract.py` 追加：

```python
BATCH_WRAPPER = ROOT / "scripts" / "run_batch.ps1"


def test_batch_wrapper_exposes_required_parameters():
    text = BATCH_WRAPPER.read_text(encoding="utf-8")

    assert "[string]$TemplateDir" in text
    assert "[string]$SourceDir" in text
    assert "[string]$SourceFile" in text
    assert "[ValidateSet(\"overwrite\", \"merge\")]" in text
    assert "[string]$FrameworkRoot" in text
    assert "[string]$PromptContract" in text
    assert "[string]$PythonExe = \"python\"" in text
    assert "[string]$Output" in text


def test_batch_wrapper_calls_batch_plan_cli_only():
    text = BATCH_WRAPPER.read_text(encoding="utf-8")

    assert '"batch-plan"' in text
    assert "--template-dir" in text
    assert "--source-dir" in text
    assert "--source-file" in text
    assert "--framework-root" in text
    assert "--prompt-contract" in text
    assert "extract-candidates" not in text
    assert "make-review-pack" not in text
    assert "render" not in text


def test_batch_wrapper_runs_with_chinese_and_space_paths(tmp_path):
    powershell = shutil.which("powershell") or shutil.which("pwsh")
    if powershell is None:
        pytest.skip("PowerShell is required to exercise run_batch.ps1")
    template_dir = tmp_path / "模板 目录"
    source_dir = tmp_path / "凡人 修仙传"
    template_dir.mkdir()
    source_dir.mkdir()
    source_file = source_dir / "凡人修仙传.txt"
    source_file.write_text("韩立服下黄龙丹。", encoding="utf-8")
    (template_dir / "README.md").write_text("# 元规则\n", encoding="utf-8")
    (template_dir / "丹药分析模板.md").write_text(
        "# 丹药分析模板\n\n## 推荐结构\n\n| 丹药名称 | 功效 | 证据 |\n| --- | --- | --- |\n",
        encoding="utf-8",
    )

    command = [powershell, "-NoProfile"]
    if Path(powershell).name.lower() == "powershell.exe":
        command += ["-ExecutionPolicy", "Bypass"]
    result = subprocess.run(
        [
            *command,
            "-File",
            str(BATCH_WRAPPER),
            "-TemplateDir",
            str(template_dir),
            "-SourceDir",
            str(source_dir),
            "-SourceFile",
            str(source_file),
            "-PythonExe",
            sys.executable,
        ],
        cwd=ROOT,
        text=True,
        encoding="utf-8",
        capture_output=True,
        check=False,
    )

    assert result.returncode == 0, result.stderr
    stdout = json.loads(result.stdout.splitlines()[-1])
    output = source_dir / "batch-plan.json"
    assert stdout["output"] == str(output)
    plan = json.loads(output.read_text(encoding="utf-8"))
    assert plan["template_count"] == 1
    assert Path(plan["items"][0]["output_path"]).name == "丹药分析.md"
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_generic_wrapper_contract.py::test_batch_wrapper_exposes_required_parameters tests/test_generic_wrapper_contract.py::test_batch_wrapper_calls_batch_plan_cli_only -q
```

Expected: FAIL，`scripts/run_batch.ps1` 不存在。

- [ ] **Step 3: 创建 wrapper**

创建 `scripts/run_batch.ps1`：

```powershell
param(
    [Parameter(Mandatory = $true)]
    [string]$TemplateDir,

    [Parameter(Mandatory = $true)]
    [string]$SourceDir,

    [Parameter(Mandatory = $true)]
    [string]$SourceFile,

    [ValidateSet("overwrite", "merge")]
    [string]$Mode = "overwrite",

    [string]$FrameworkRoot = "",
    [string]$PromptContract = "",
    [string]$PythonExe = "python",
    [string]$Output = ""
)

$ErrorActionPreference = "Stop"
$env:PYTHONIOENCODING = "utf-8"
$env:PYTHONDONTWRITEBYTECODE = "1"
[Console]::OutputEncoding = [Text.Encoding]::UTF8
$OutputEncoding = [Text.Encoding]::UTF8
chcp 65001 > $null

$scriptRoot = Split-Path -Parent $MyInvocation.MyCommand.Path
$pipeline = Join-Path $scriptRoot "worldbuilding_pipeline.py"

if (-not $FrameworkRoot) {
    $FrameworkRoot = Join-Path $SourceDir ".worldbuilding-framework"
}
if (-not $PromptContract) {
    $pluginRoot = Split-Path -Parent $scriptRoot
    $PromptContract = Join-Path $pluginRoot "assets\batch-prompt-contract.yaml"
}
if (-not $Output) {
    $Output = Join-Path $SourceDir "batch-plan.json"
}

$arguments = @(
    "batch-plan",
    "--template-dir", $TemplateDir,
    "--source-dir", $SourceDir,
    "--source-file", $SourceFile,
    "--mode", $Mode,
    "--framework-root", $FrameworkRoot,
    "--prompt-contract", $PromptContract,
    "--output", $Output
)

& $PythonExe -B $pipeline @arguments
if ($LASTEXITCODE -ne 0) {
    throw "batch-plan failed with exit code $LASTEXITCODE"
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_generic_wrapper_contract.py -q
```

Expected: PASS。

- [ ] **Step 5: 可选提交（仅用户要求提交时执行）**

```powershell
git -c core.quotePath=false add scripts/run_batch.ps1 tests/test_generic_wrapper_contract.py
git commit -m "feat: add batch plan wrapper"
```

---

## 最终验证

- [ ] **Step 1: 运行聚焦测试**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest tests/test_skill_workflow_doc.py tests/test_analysis_framework.py tests/test_template_profile.py tests/test_template_router.py tests/test_batch_plan.py tests/test_generic_pipeline_cli.py tests/test_generic_wrapper_contract.py -q
```

Expected: PASS。

- [ ] **Step 2: 运行全量测试**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
pytest -q
```

Expected: PASS。若出现 `LF will be replaced by CRLF` 或 `CRLF will be replaced by LF`，只记录为 Git 换行提示，不作为测试失败。

- [ ] **Step 3: 手工冒烟生成 batch plan**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
$repo = 'C:\Users\Administrator\plugins\cultivation-worldbuilding-research'
$smoke = Join-Path $env:TEMP ("cultivation-batch-smoke-" + [guid]::NewGuid().ToString("N"))
$templateDir = Join-Path $smoke 'templates'
$sourceDir = Join-Path $smoke 'source'
New-Item -ItemType Directory -Path $templateDir, $sourceDir | Out-Null
Set-Content -LiteralPath (Join-Path $templateDir 'README.md') -Encoding UTF8 -Value '# 元规则'
Set-Content -LiteralPath (Join-Path $templateDir '丹药分析模板.md') -Encoding UTF8 -Value @'
# 丹药分析模板

## 推荐结构

| 丹药名称 | 功效 | 证据 |
| --- | --- | --- |
'@
Set-Content -LiteralPath (Join-Path $templateDir '事件因果链（长程因果图）模板.md') -Encoding UTF8 -Value @'
# 事件因果链（长程因果图）模板

## 推荐结构

1. 起因：
2. 发展：
3. 结果：

起因 -> 发展 -> 结果
'@
Set-Content -LiteralPath (Join-Path $sourceDir '凡人修仙传.txt') -Encoding UTF8 -Value '韩立服下黄龙丹。'
& 'C:\Users\Administrator\plugins\cultivation-worldbuilding-research\scripts\run_batch.ps1' `
  -TemplateDir $templateDir `
  -SourceDir $sourceDir `
  -SourceFile (Join-Path $sourceDir '凡人修仙传.txt') `
  -Mode overwrite
```

Expected: 输出 JSON 指向 `$sourceDir\batch-plan.json`；该文件中 `template_count` 为 `2`，每个 item 的 `subagent_prompt` 只包含一个模板全文。

- [ ] **Step 4: 自检实施要求**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git -c core.quotePath=false status --short
git -c core.quotePath=false diff --check
```

Expected: `diff --check` 无真实 whitespace error；`status --short` 只显示本计划相关文件。

- [ ] **Step 5: 可选最终提交（仅用户要求提交且前面未逐任务提交时执行）**

```powershell
git -c core.quotePath=false add skills/extracting-worldbuilding/SKILL.md assets/framework-presets.yaml assets/template-registry.yaml assets/batch-prompt-contract.yaml scripts/pipeline/template_profile.py scripts/pipeline/template_router.py scripts/pipeline/batch_plan.py scripts/worldbuilding_pipeline.py scripts/run_batch.ps1 tests/test_skill_workflow_doc.py tests/test_analysis_framework.py tests/test_real_template_shapes.py tests/test_template_profile.py tests/test_template_router.py tests/test_batch_plan.py tests/test_generic_pipeline_cli.py tests/test_generic_wrapper_contract.py tests/test_pipeline_cli.py
git commit -m "feat: orchestrate template-driven batch generation"
```

---

## 自审记录

**Spec coverage:**
P0-1 对应 Task 1；P0-2 对应 Task 2；P1-1 对应 Task 3；P1-2 对应 Task 4；P2-1 字面交付是 `scripts/run_batch.ps1`，对应 Task 7；Task 5 和 Task 6 是为了保持 PowerShell wrapper 薄封装而新增的可测试支撑层；P2-2 对应各任务中的测试步骤与“最终验证”。

**Placeholder scan:**
计划未使用禁用占位表达。领域内的“待核验”是输出规则，不是实施占位。

**Type consistency:**
`batch_plan.py` 中 JSON 字段统一使用 `subagent_prompt`、`report_shape`、`framework_outputs`、`output_exists`；CLI stdout 使用 `output`、`items`、`mode`；测试与实现命名一致。

**硬编码检查:**
模板名到 shape 的兜底映射集中在 YAML；prompt contract、mode contract、质量基线、事实标签和检索方式集中在 `assets/batch-prompt-contract.yaml`；丹药 registry 不再作为活动逻辑；Python 中保留的 `overwrite/merge` 是受限模式枚举，不包含项目领域数据。
